@page
@model RazorMedBilleder.Pages.Images.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Image</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Image.name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Image.name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Image.imageData)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Image.imageData)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Image?.id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
